# skylens_model.utils

Small stdlib-only helpers shared across model code.

- `geo.py` — GPS <-> local ENU meter conversion, a pure-Python mirror of
  `src/skylens_core/geo.ts`. Keep the two files numerically in sync.
