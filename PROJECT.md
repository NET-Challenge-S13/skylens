---
tags:
  - project
  - 넷챌린지
  - 중간평가
  - prototype
  - three.js
  - gaussian-splatting
parent: '[[넷 챌린지 캠프]]'
related: '[[ARCHITECTURE.md]], [[IDEA.md]]'
---

# PROJECT.md — SkyLens 중간 평가 프로토타입

> 최종 평가에서는 실제 드론을 띄워 KOREN·Core HPC·Edge VM으로 이어지는 풀 파이프라인을 시연한다.
> 중간 평가(약 1개월)는 하드웨어·인프라 없이도 **제어·상황판 두 역할의 협력 UX와 3D 복원 + AI 탐지 오버레이의 일관성이 "실제로 작동한다"는 확신**을 심사위원에게 주는 것이 목표다.
> 이 문서는 그 1개월짜리 프로토타입의 구현 계획을 상세히 서술한다.

---

## 0. 이 프로토타입이 증명해야 하는 것 / 증명하지 않아도 되는 것

중간 평가는 최종 평가의 축소판이 아니라 **다른 것을 증명하는 별도의 데모**다. 이 구분을 먼저 명확히 해야 구현 범위가 산다.

| | 증명 대상 | 이 프로토타입에서 |
|---|---|---|
| **최종 평가** | KOREN·Core HPC가 실제로 필요하다 (성능 대조) | ❌ 증명 안 함 |
| **최종 평가** | 실제 드론이 실시간으로 영상을 보낸다 | ❌ 증명 안 함 (오프라인 데이터로 대체) |
| **중간 평가** | 3D 복원 결과가 실제 현장을 정확히 재현한다 | ✅ 사전 촬영·gsplat으로 증명 — **정량 결과 §7.1** |
| **중간 평가** | **관제탑이 오퍼레이터의 "경로 지정 → 리더 비행 → 영상 추적"을 실제로 처리한다** | ✅ GPS 경로 계획 UI + 드론 시뮬레이션 |
| **중간 평가** | **지휘관(현황판)이 서버 스트림(splat 청크 + 탐지 결과)을 받아 3D 상황판으로 표시한다** | ✅ 서버 인터페이스 + 청크 축적 재생 |
| **중간 평가** | 두 역할 간의 WebRTC 동기화와 카메라 협력이 자연스럽다 | ✅ P2P + 상태 공유로 증명 |

즉 중간 평가에서 심사위원이 봐야 하는 것은 **"관제탑과 현황판이라는 두 역할이 실제로 협력 가능하다"**는 신뢰이지, **"KOREN·HPC 없이도 작동한다"**는 뜻이 아니다(그들은 선택적). 이 구분 때문에 아래 구현 계획에서는 KOREN/일반망 성능 대조, Core HPC 실시간 성능, 드론 하드웨어 같은 요소를 의도적으로 제외하고 **오프라인 데이터에 기반한 관제탑/현황판 두 클라이언트 제어·상황판 협력**에 집중한다.

---

## 1. 데모 컨셉 요약

- **화면 구성**: 컴퓨터 2대 (또는 모니터 2개), 각각 독립 실행 또는 같은 브라우저의 두 탭
  - **관제탑**: 오퍼레이터가 **GPS 웨이포인트를 통해 드론 경로를 지정**하는 제어 인터페이스. 경로 지정 후 리더 드론이 그 경로를 따라 비행하고 군집 드론이 동행하며, 메인 드론의 카메라 영상을 패널로 확인. 방향키 수동 조향(override) 가능. **WebRTC(PeerJS)로 현황판과 동기화**.
  - **현황판**: 서버가 progressively push하는 **Gaussian 스플랫 청크**가 도착하면 3D 공간이 계속 확장되고, **서버의 인간 탐지 결과(GPS 좌표)**가 오면 3D 위에 마커로 표시. 현재 드론 텔레메트리를 따라 카메라도 동기화. 탐지 마커 클릭 시 해당 지점으로 자동 줌인. **미니맵 + 서버 수신 상태 표시**.
- **서버 연결**: 현재는 **인터페이스 + mock provider**로 구현돼 있어(serverSource.ts) 자체 완결형 데모 가능. 실 백엔드는 `serverSource.connect(url)` 지점에 교체 연결.
- **연출 의도**: 관제탑은 일부러 "로우파이" 드론 시뮬레이션처럼 보여줘서(기하학적 형태, 저정보량 렌더링), 현황판이 같은 현장을 **실사 Gaussian 스플랫 + 탐지 마커로 지휘관 상황판**으로 표현하는 대비를 극적으로 만든다. 오퍼레이터의 지시 → 드론 비행 → 그 궤적 따라 3D 확장 → AI 탐지 라는 서사 흐름이 두 화면 모두에 일관되게 보인다.
- **기술 구조**: 관제탑·현황판은 **서로 다른 클라이언트**(src/skylens_core/control.ts, src/skylens_client/status.ts)로 구현되어 WebRTC 신호링 + 상태 공유로 연결. 둘은 같은 GPS 좌표계(GeoAnchor)와 Gaussian 스플랫 데이터를 공유하되, 관제탑은 제어·시뮬레이션 초점, 현황판은 지휘관 상황판 초점으로 렌더링한다.

---

## 2. 사전 준비 (구현 이전, 오프라인 작업)

1. **촬영**: 시연할 실내·모형 공간을 정하고, **카메라 3대(또는 스마트폰 3대)로 전체 공간을 여러 시점에서 촬영**한다. 실제 드론 대신 사람이 들고 걷거나 짐벌 리그를 써도 무방 — 목적은 충분한 시점 다양성으로 3D 복원 가능하게 하기.
2. **가우시안 스플래팅 생성** — ✅ **완료**. 포즈 추정 → gsplat(Nerfstudio) 학습 → `.ply` export. 이 사전 작업 자체가 "우리 파이프라인이 실제로 동작함"의 증거이며, 정량 결과는 §7.1에 정리했다. 중간 평가에서는 전체 스플랫을 한 번에 로드하지만, 실제 서버 연결 시에는 이 스플랫을 **시간별 청크로 분할해 progressively 스트리밍**하는 변환만 추가하면 된다.

   > ⚠️ **실제 구현은 GLOMAP이 아니라 COLMAP 4.1이다.** `ARCHITECTURE.md`·`IDEA.md` 등 상위 문서는 GLOMAP으로 적혀 있으나, 실제로는 **ALIKED + LightGlue 특징점이 내장된 COLMAP 4.1**을 썼다. 실내 저텍스처 장면에서 기본 SIFT로는 540장이 3개 모델로 쪼개졌고, ALIKED로 바꾸자 540장 전부가 하나로 붙었기 때문이다(§7.1 ①). 상위 문서의 GLOMAP 표기는 정정 대기 상태다.
3. **탐지 라벨링**: 최종 파이프라인의 UNet 추론은 이 단계에서는 스킵하고, 사람 인스턴스가 위치할 지점 2~3곳을 수동으로 3D GPS 좌표로 지정해 마커 데이터(JSON)를 만든다. 각 마커는 `{ latitude, longitude, altitude, confidence, type }` 형태로 서버 응답을 mock. (UNet이 이미 준비돼 있다면 실제 추론 결과를 써도 되지만, 필수는 아님 — §7의 확장 옵션 참조)
4. **좌표계 정리**: 스플랫의 월드 좌표와 드론 경로 좌표를 **공통 GPS 프레임(GeoAnchor)**으로 맞춘다(geo.ts 참조). 각 스플랫 청크마다 explicit align transform(pos/rot/scale)을 지정해 정렬. 이게 안 맞으면 "드론이 지나가며 3D가 생긴다"는 서사가 어긋난다.

---

## 3. 기술 스택

| 영역 | 기술 | 비고 |
|---|---|---|
| 3D 렌더링 | **Three.js** | 관제탑·현황판 공통 |
| 가우시안 스플랫 로딩 & 청크 스트리밍 | **@mkkellogg/gaussian-splats-3d** | `.ply`/`.splat` 로드, 서버 청크로 progressive 로드 구현 ([splat chunk format](../figures/splat-protocol.png)) |
| 관제탑 드론 시뮬레이션 | GPS 경로 좌표 → ENU 변환 + 오일러 보간 | §4 참조; 리더+군집 구현 |
| 관제탑 로우파이 렌더링 | 포인트클라우드 다운샘플링 + `PointsMaterial` / 커스텀 쉐이더 | 기하학적 저정보량 표현; "시뮬레이션"으로 의도된 스타일 |
| 현황판 서버 인터페이스 | **serverSource.ts** (interface + mock provider) | 실제 서버는 `connect(url)` 지점에 교체; 청크 스트림 + 탐지 결과 mock |
| 현황판 progressive reveal | 서버 청크 도착 → 씬에 add → 연속 알파 페이드인 | 드론 시뮬 위치와 무관, 순전히 서버 수신 기반 |
| 두 클라이언트 간 동기화 | **WebRTC (PeerJS + Google STUN)** | §8 참조; 같은 `?room=` 값이면 자동 연결 |
| GPS ↔ ENU ↔ 씬 좌표 변환 | **geo.ts** (TS) + **geo.py** (Python 미러, 수치 동기) | GeoAnchor 중심의 로컬 미터 프레임 |
| 상태관리 | 바닐라 JS + store 모듈 또는 경량 상태 스토어 | React 불필요, 순수 Three.js + DOM 앱 |
| 실행 환경 | **Vite dev server** (LAN 노출) | npm run dev → 다른 컴퓨터에서도 접속 가능 |
| 인터넷 | 불필요 (로컬 네트워크 + STUN만 사용) | 실 백엔드 연결 시까지는 오프라인 완결 |

---

## 4. 관제탑 & GPS 경로 계획

### 4.1 경로 계획 UI (routeModal.ts)

**핵심 기능**: 오퍼레이터가 **실제 GPS 좌표(위도/경도/고도)로 웨이포인트를 추가하고, 지정한 경로를 리더 드론에 배정**한다.

- **지도 인터페이스** 또는 **GPS 입력 폼**: 목표 지점들을 마크. 각 웨이포인트는 `{ lat, lon, alt, heading(선택) }` 형태.
- 경로 배정 버튼을 누르면, 그 좌표들이 **ENU 로컬 좌표로 변환**(geo.ts 이용, GeoAnchor 기준)되어 리더 드론의 pathFollower에 할당.
- 배정 후: 리더 드론이 그 경로를 따라 비행, 군집 드론들이 동행.
- **프리셋 데모 경로** 옵션도 제공 (빠른 시연용): 버튼 클릭 → 미리 정해진 경로가 자동 로드 & 배정.

### 4.2 수동 조작 & 안전 복귀

- **기본 동작**: 배정된 경로를 따라 자동 비행.
- **수동 조작**: 방향키(↑↓←→, Q/E 고도) 누르면 **현재 활성 드론**(탭 키로 전환, 1/2/3 숫자키 가능)이 경로에서 일시 이탈해 수동 조종.
- **자동 복귀**: 수동 입력이 1.5초 이상 없으면, 현재 위치에서 **가장 가까운 미래 웨이포인트로 부드럽게 트윈 복귀**. 이를 통해 발표자가 라이브 조작 시연 중에도 안정성을 보장.
- **상태 머신**: `AUTOPILOT → MANUAL → RETURNING → AUTOPILOT` — 매 프레임 업데이트하는 간단한 로직.

### 4.3 드론 시뮬레이션 & 리더/군집 구조

- **리더 드론(Lead)**: 배정된 경로를 정확히 따름. 경로는 Catmull-Rom 스플라인으로 웨이포인트 사이 보간 → 자연스러운 곡선 비행.
- **군집 드론(Companions)**: 리더를 따라가되, 일정한 상대 거리/방위를 유지(예: 리더로부터 -3m 좌측, +2m 위). `pathFollower.ts`에서 구현.
- **타이밍**: 3개 드론을 동시에 시뮬하면 "전체 공간을 분할 탐색하는" 인상 강화.

### 4.4 관제탑 화면 렌더링 (로우파이 "시뮬레이션" 스타일)

**연출 의도**: 현황판의 실사 3D와 대비시키기 위해 의도적으로 "저정보량·기계적 느낌"으로 표현.

- **배경**: 깊은 남색(`#0a1128` 계열) 단색 또는 은은한 그리드/파티클.
- **지형·구조물**: 전체 가우시안 스플랫을 로드하지만, **포인트를 다운샘플링**한 포인트클라우드로 표현. `PointsMaterial` + 청록~남색 그라데이션 에미시브 → "스캔·센싱 중" 느낌.
- **드론**: 단순한 로우폴리 모델(피라미드/구체 + 발광 효과) 또는 프로펠러 파티클. 이동 궤적을 옅은 트레일 라인으로 표시 → "탐색 경로"를 시각적으로 강조.
- **카메라**: 리더 드론을 **3인칭 추적**(드론 뒤 약 3-5m, 위쪽에서 진행 방향 바라봄). `lerp` + `lookAt`으로 부드럽게 갱신.
- **정보 패널**: 우측 상단에 "리더 드론 / 군집 드론" 상태, 현재 경로 진도(%).
- 이 화면은 **정보를 의도적으로 줄이는 연출**이므로, 텍스처·고충실도 라이팅은 오히려 불필요.

---

## 5. 현황판 & 서버 스트림 기반 3D 축적

### 5.1 기본 아키텍처 (serverSource.ts + splatScene.ts)

현황판은 **서버로부터 progressively push되는 Gaussian 스플랫 청크**를 수신해 3D 공간을 계속 확장한다. 청크는 **구간(segment) × 수준(level)** 두 축을 가진다 — **딜레이 패턴**(중간보고서 Ⅱ-3-다).

- **딜레이 패턴**: 드론이 지나간 구간부터 낮은 학습 스텝(수준 1)의 결과를 즉시 확정해 띄우고, 뒤이어 상위 수준으로 정제한다. 앞 구간이 정제되는 동안 다음 구간의 수준 1이 도착하므로 여러 구간이 서로 다른 수준으로 동시에 존재한다. 타이밍은 `CONFIG.delayPattern`(`segmentPeriod`, `levelDelays`).
- **청크 기반 로딩**: 전체 스플랫을 한 번에 로드하지 않고 구간 단위로 수신. 각 청크는 `{ id, url, align, segment, level, steps, label, final }` 형태(protocol.ts).
- **누적과 교체**: 서로 다른 구간은 나란히 누적하고, 같은 구간의 상위 수준은 하위 수준을 **교체**한다(`splatScene.addChunk` → add 후 이전 수준 `removeSplatScene`). 처리가 밀려 이미 추월당한 수준은 큐에서 건너뛴다.
- **데모 자산**: `split_segments.py`가 학습 스텝별 PLY를 같은 공간 경계로 잘라 `res/static/demo/segments/` + `segments.json`을 만든다. 자산이 없으면 단일 장면 스트림으로 폴백.
- **시간 지연 없음**: 서버 수신이 즉시 표시됨. (최종 평가에서는 지연을 시뮬할 수 있으나, 중간 평가에서는 "지금 도착한 데이터"를 즉시 표시)
- **서버 인터페이스**: 현재 `serverSource.ts`는 mock provider로 구현됨. 실 백엔드는 WebSocket/gRPC streaming으로 교체 가능.

### 5.2 청크 수신 & 알파 페이드인

각 청크 도착 시:
1. 청크를 `opacity: 0`으로 씬에 add.
2. 0.5~1초 동안 opacity 0→1 페이드인(ease-out).
3. "지금 막 복원 중"이라는 시각적 신호 강화 — 선택적으로 emissive flash 또는 부드러운 파티클 이펙트 추가.

**진행 상황 표시**: 화면 좌상단에 "복원도 X/Y청크" 또는 진행률 바를 표시.

### 5.3 드론 텔레메트리 추적 & 카메라 동기화

- **관제탑 클라이언트로부터**: WebRTC를 통해 리더 드론의 현재 위치(GPS/ENU) 실시간 수신.
- **현황판 카메라**: 평상시는 드론의 현재 위치를 추적(3인칭, 약 5m 뒤/위). 관제탑의 "드론이 지나가는" 경로를 시각적으로 따라가게 됨.
- **카메라 동기화**: "§8 카메라 상태 머신" 참조. 기본은 SYNCED 상태에서 드론 위치 추적.

### 5.4 탐지 마커 수신 & UI 표시

서버로부터 **탐지 결과(GPS 좌표 + confidence + type)** 수신 시:

1. 해당 GPS 좌표를 ENU로 변환 → 3D 씬의 마커 위치 결정.
2. 마커 아이콘(예: 🔴 위험 / 🟢 사람 + pulsing ring)과 라벨("생존자 추정 위치", "신뢰도 0.92")을 3D 공간에 표시.
3. 동시에 **UI 오버레이 패널**(HTML/CSS, 하단 우측)에 "탐지 알림: 사람 감지 1명" 같은 텍스트 출력 + **"해당 위치로 이동" 또는 "확인" 버튼** 제공.
4. 버튼 클릭 시: 카메라가 해당 마커로 자동 이동 & 줌인 → "§8의 FOCUSING 상태" 진입.

**서버 수신 기간**: 아직 탐지 결과를 받지 못한 경우, "⏳ 탐지 대기 중" 상태를 미니맵/상태창에 표시.

### 5.5 전체 시각 구성

- **3D 씬** (메인 우측 영역): 누적되는 스플랫 청크 + 드론 위치 신호 + 탐지 마커 + 고도 격자.
- **미니맵** (좌상단): 위성지도 스타일, 드론 위치(⬆), 탐지 마커(🔴🟢), 스플랫 커버리지 범위.
- **서버 상태** (우상단): "청크 수신: 5/10", "탐지 대기", "WebRTC: 연결됨", "최근 업데이트: 2초 전".
- **카메라 제어**: 마우스 우클릭 드래그로 자유 회전 + 스크롤 줌 인/아웃.
- **정보 패널** (하단): 현재 탐지 목록, 각 마커별 신뢰도, "확인" 버튼.

---

## 6. 위험구역 오버레이 (선택 — 시간 여유 시)

- ARCHITECTURE.md의 세그멘테이션 헤드(위험구역=stuff)에 대응하는 연출로, 서버로부터 위험 영역의 마스크/복셀을 수신 시 특정 영역을 반투명 붉은 디컬(decal) 또는 볼륨으로 3D 공간에 오버레이해 "이 구역은 위험"임을 표시.
- **우선순위 낮음**: 중간 평가 핵심 서사는 "경로 지정 → 드론 비행 → 3D 축적 → AI 사람 탐지"이므로, 시간이 부족하면 이 항목은 발표 멘트("최종 평가에서는 세그멘테이션 모델도 함께 실행되어 위험구역 마스크를 실시간 표시합니다")로 대체 가능.
- **구현**: 서버 mock에 `getHazardZones()` 엔드포인트 추가 → 수신된 폴리곤/복셀을 Three.js `DecalGeometry` 또는 반투명 메시로 렌더.

---

## 7. 연출과 실제의 경계 — 정직한 프로토타입 설계

이 프로토타입은 "완전 자동 데모"가 아니라, **검증된 부분과 향후 개발 부분을 명확히 구분하는 정직한 데모**다. 발표 시 이 경계를 명시하면 심사위원의 신뢰도가 오히려 높아진다:

| 부분 | 상태 | 설명 |
|---|---|---|
| **3D 복원 (Gaussian Splatting)** | ✅ 실제 구현 · **정량 검증** | 실촬영 영상 3편(540프레임)을 COLMAP→gsplat으로 복원. **PSNR 23.62 / SSIM 0.907 / 가우시안 653,353개.** 여기에 더해 학습 조건 28개로 설계 파라미터를 측정했다 → §7.1 |
| **관제탑 (경로 지정 & 드론 시뮬)** | ✅ 실제 구현 | GPS 웨이포인트 지정 → ENU 변환 → 경로 재생 → 리더/군집 드론 시뮬. 오퍼레이터 입력이 드론 움직임에 즉시 반영. |
| **현황판 서버 인터페이스** | ⚠️ Mock 구현 | 실 백엔드 없이 serverSource.ts의 mock provider가 청크/탐지 결과를 시뮬레이션. 실 서버 연결은 `serverSource.connect(url)` 지점만 교체하면 됨. |
| **탐지 마커 좌표** | ⚠️ 수동 지정 | 현재는 §2에서 미리 지정한 GPS 좌표. UNet 모델이 준비되면 실제 추론 결과로 교체 가능. |

**발표 스크립트 권장사항**:
- "3D 복원은 실제 촬영 데이터로 Gaussian Splatting 파이프라인을 거쳐 생성한 결과입니다. 완전히 실제입니다. 540프레임에서 가우시안 65만 개, PSNR 23.62를 얻었고, 여기에 더해 **학습 조건 28개를 돌려 특징점 방식·포즈 정확도 요구사항·첫 화면 출력 시점을 실측으로 정했습니다**(§7.1)."
- "경로 계획과 드론 제어 UI는 GPS 좌표 기반으로 완전히 동작합니다. 오퍼레이터가 실제로 드론을 제어할 수 있습니다."
- "현재 이 데모는 오프라인 서버(mock)로 실행되고 있으며, 실제 KOREN·Core HPC 백엔드와의 연결은 최종 평가 단계에서 이루어집니다. 하지만 프로토콜과 인터페이스는 이미 정의되어 있어, 실제 백엔드 추가는 최소한의 변경만 필요합니다."
- "탐지 마커는 현재 시연용으로 수동 지정했으나, UNet 모델 개발이 완료되면 실제 추론 결과를 실시간으로 대입할 수 있습니다."

이런 식으로 **무엇이 실제이고 무엇이 향후 개발 예정인지 명확히 하는 것**이 오히려 프로토타입의 신뢰도를 높인다.

### 7.1 3D 복원 — 무엇을 어디까지 검증했나

> 원자료: [`src/skylens_model/models/skylens/RESULTS.md`](src/skylens_model/models/skylens/RESULTS.md) ·
> 설계 근거: 같은 폴더 [`README.md`](src/skylens_model/models/skylens/README.md) ·
> 결과를 만든 스크립트: 같은 폴더 `scripts/`

**대상**: 실내 복도. 같은 공간을 세 번 지나며 촬영한 영상 3편을 하나의 좌표계로 통합.
2026-08-09 ~ 08-16, 로컬 GPU.

| | 값 |
|---|---|
| 입력 | 540 프레임 @2048px (영상 3편) |
| 재구성 | **540 / 540 전부 단일 모델에 등록**, 기하 검증 통과율 91% |
| 복원 | 가우시안 **653,353개** |
| 품질 | **PSNR 23.62 · SSIM 0.907 · LPIPS 0.301** (30,000스텝) |
| 소요 | COLMAP 1시간 37분 + 학습 37분 52초 |

여기까지가 "복원이 된다"의 근거다. 그런데 **한 번 돌려본 것으로는 설계 근거가 안 된다.**
학습 조건을 28개 돌려 아래 다섯 가지를 측정했고, 이 값들이 곧 시스템 요구사항이 된다.

**① 특징점 방식 — ALIKED + LightGlue 채택 (기획서의 SIFT 기본값에서 변경)**

| | SIFT | ALIKED + LightGlue |
|---|---|---|
| 기하 검증 통과율 | 56% | **91%** |
| 등록 결과 | **3개 모델로 분열** (최대 399장) | 540/540 단일 |
| 왜곡계수 k₁ | +0.4482 (발산) | +0.0778 |
| 매퍼 소요 | 1시간 7분 | **54분** |

실내 저텍스처(흰 벽·반사 바닥·반복 구조)에서 SIFT는 특징점을 3.3배 많이 잡지만
절반이 검증에서 탈락하고, 살아남은 오매칭이 자기보정을 발산시킨다.
**정확도와 속도 양쪽에서 ALIKED가 이겼다.**

**② 점진적 출력 — "30초는 갱신 주기" 구조가 성립한다 (§4 재정의의 실측 근거)**

| 스텝 | PSNR | 가우시안 | 경량 PLY |
|---:|---:|---:|---:|
| 250 | 16.09 | 12,708 | 0.7 MB |
| 1,000 | 18.47 | 19,368 | 1.0 MB |
| 7,000 | 21.48 | 316,057 | 16.9 MB |
| 30,000 | **23.62** | 653,353 | 34.9 MB |

곡선이 로그 형태라 초반에 대부분을 벌어들인다 — 250→1,000에서 2.38dB 오르는 동안
10,000→30,000에서는 1.60dB뿐이다. **전체의 3%인 1,000스텝에서 최종과 5.15dB 차이**까지
좁혀지므로, 낮은 스텝으로 첫 화면을 띄우고 계속 개선하는 구조가 실제로 가능하다.
전송량이 0.7MB → 34.9MB로 **50배** 벌어지는 것도 §5 청크 스트리밍 설계의 근거다.

**③ 포즈 정확도 — 드론 위치 요구사항을 수치로 확정**

```
자세 0.1°  −0.08 dB      위치 0.2%  −2.24 dB
자세 1.0°  −2.70 dB      위치 0.5%  −3.60 dB
```

**위치 오차가 자세 오차보다 훨씬 치명적**이다. 위치 0.2%의 손해를 자세로 내려면 0.8°가 필요하다.
GPS 수평 오차(0.5~1.5m)는 이 규모를 크게 웃돌므로,
**드론 위치 텔레메트리를 카메라 포즈로 직접 쓸 수 없다는 것이 실측으로 확정됐다.**
포즈는 영상에서 다시 푸는 현재 설계가 맞다.

**④ 증분(누적) 복원 — 가능하다는 판정** (2026-08-07 회의 액션 아이템)

회의록의 *"누적 복원이 불가능하면 가우시안 스플래팅 자체를 재설계해야 함"* 우려에 대한 답이다.
웜스타트가 처음부터 학습보다 6.5dB 나빴는데, **원인은 알고리즘이 아니라 좌표계**였다 —
데이터 로더가 카메라 집합에서 정규화 변환을 매번 새로 계산해 배치 간 87.8° 틀어져 있었다.
첫 배치의 변환을 고정하면 해결된다. **재설계는 불필요하다.**

**⑤ 편대 설계 — 판정 보류 (정직하게 남긴다)**

간격 vs 시선각 6조건을 돌렸으나 **조건마다 평가 세트가 달라 PSNR을 비교할 수 없다.**
공통 평가 세트로 재실행해야 판정 가능하다. 다만 평가 방식과 무관한 가우시안 수에서는
간격 넓은 조합이 270장으로 472,103개, 시선각만 벌린 조합이 405장으로 322,791개를 만들어
**간격 쪽이 유력하다는 정황**이 나왔다.

> **발표에서 이렇게 말할 수 있다**: "복원이 된다는 것뿐 아니라, 어떤 특징점을 써야 하는지,
> 드론 위치를 얼마나 정확히 알아야 하는지, 몇 스텝에서 첫 화면을 띄울 수 있는지를
> 실측으로 정했습니다. 편대 간격 실험은 평가 설계에 결함이 있어 아직 판정하지 않았습니다."

---

## 8. 관제탑 ↔ 현황판 동기화 & 카메라 협력

### 8.1 동기화 대상

- **관제탑 → 현황판**: 리더 드론의 현재 위치/방향(GPS or ENU) → 실시간 수신. 현황판 카메라가 이를 추적.
- **현황판 → 관제탑**: (양방향은 아님) 현황판에서의 사용자 조작(탐지 마커 클릭 등)은 로컬 이벤트로만 처리. 관제탑은 자신의 드론 시뮬만 담당.
- **공유 채널**: WebRTC(PeerJS) DataChannel을 이용해 `?room=` 기반으로 자동 연결.

### 8.2 카메라 상태 머신 (현황판 쪽)

```
[SYNCED]  현황판 카메라가 관제탑으로부터 수신한 리더 드론 위치를 추적
            (드론 뒤 약 5m, 위쪽에서 진행 방향 바라봄)
   │  탐지 마커 수신 또는 UI 버튼 클릭
   ▼
[FOCUSING]  카메라를 탐지 좌표로 트윈 이동/줌인
            (ease-in-out, 1.5~2초)
   │  트윈 완료
   ▼
[LOCKED]  탐지 좌표에 카메라 고정, UI에 "탐지 확인" 버튼 표시
   │  "탐지 확인" 버튼 클릭
   ▼
[RETURNING]  카메라를 "현재 시점의 관제탑 리더 드론 상태"로 트윈 복귀
             (ease-in-out, 1.5~2초)
   │  트윈 완료
   ▼
[SYNCED]  (다시 추적 모드로)
```

**중요**:
- `LOCKED` 상태에서도 관제탑은 계속 진행되므로(드론이 계속 날아감), `RETURNING`의 목표는 **버튼을 누른 그 시점의 관제탑 최신 드론 상태**여야 한다 — 탐지 발생 당시 상태로 되돌아가면 안 됨.
- `FOCUSING`/`RETURNING` 시간을 1.5~2초로 설정하면, 지휘관에게 상황을 인지하고 마커를 확인할 충분한 시간을 주면서도 발표 리듬을 너무 끊지 않는다.

### 8.3 상태 공유 데이터 & 프로토콜

**관제탑 → 현황판** (지속적 스트림):
```json
{
  "type": "LEADER_POSITION",
  "leaderPos": [x_enu, y_enu, z_enu],
  "leaderDir": [heading_deg],
  "timestamp": 1724000123456
}
```

**현황판 → 관제탑** (이벤트 기반, 필요시):
```json
{
  "type": "DETECTION_CLICKED",
  "markerIndex": 0,
  "timestamp": 1724000125000
}
```

**서버(Mock) → 현황판** (수신):
```json
{
  "type": "SPLAT_CHUNK",
  "chunkId": "chunk_001",
  "data": <binary>,
  "transform": { "pos": [x, y, z], "rot": [qx, qy, qz, qw], "scale": 1.0 }
}
```
```json
{
  "type": "DETECTION",
  "detections": [
    { "lat": 37.123, "lon": 126.456, "alt": 15.5, "type": "person", "confidence": 0.92 }
  ]
}
```

**지연 및 버퍼**:
- 현황판은 관제탑으로부터 드론 위치를 실시간(지연 0)으로 수신 후 카메라에 반영.
- (향후 확장) 서버 스트리밍의 지연을 시뮬할 경우, 현황판에 "시뮬 딜레이 N초" 옵션 추가 가능.

---

## 9. 구현 로드맵 (1개월)

| 주차 | 목표 |
|---|---|
| **1주차** | 촬영 완료 → COLMAP/gsplat 3D 복원 완료 → 좌표계 정리(GPS ↔ ENU) → 스플랫 뷰어 기본 로딩(단일 화면, 정적) 확인 |
| **2주차** | **관제탑 구현**: GPS 경로 계획 모달 + 리더 드론 경로 재생 + 군집 드론 + 로우파이 렌더링(포인트클라우드) + 카메라 추적. 프리셋 경로 + 수동 조작(방향키 override) 포함. |
| **3주차** | **현황판 구현**: serverSource.ts mock 완성 → 스플랫 청크 축적 로드 + 알파 페이드인 → WebRTC(PeerJS) 연결 및 관제탑과의 상태 공유 → 드론 위치 추적 카메라 동작 확인. |
| **4주차** | **서버 탐지 연동**: 탐지 마커 수신 + 3D 표시 + UI 패널 → FOCUSING/LOCKED/RETURNING 상태 머신 완성 → 리허설·연출 다듬기(색감, 트랜지션 타이밍, 관제탑 로우파이 스타일) → 발표 스크립트와 맞춰 최종 리허설 |

**선택사항 (시간 여유 시)**:
- §6(위험구역 오버레이) — 핵심 서사에 필수 아님. 우선순위 가장 낮음.
- 음성 안내 또는 사운드 이펙트 추가 (탐지 시 경고음 등).
- 사용성 개선 (경로 표시 선, 드론 상태 아이콘 등).

---

## 10. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 촬영 공간의 스플래팅 품질 부족(반사면, 저조도, 노이즈) | 높음 | 1주차 초반에 촬영·복원을 먼저 끝내서 조기 검증. 필요시 재촬영 여유 확보. 대안: 캠퍼스/실내 미니어처 공간 등 통제된 환경 활용. |
| GPS 좌표 ↔ ENU 변환 오류로 인한 정렬 실패 | 높음 | geo.ts와 geo.py 모두 동시에 검증. 테스트 좌표 세트로 수치 동일성 확인. 문서 초반(§2-4)에 조기 체크포인트 설정. |
| WebRTC(PeerJS) 연결 불안정 | 중간 | LAN 로컬 네트워크에서만 테스트하도록 제한. 필요시 WebSocket 폴백 구현. 최악의 경우 단일 앱 + 두 캔버스로 재구현(§8.2 참조). |
| 스플랫 청크 축적 시 메모리/성능 저하 | 중간 | 청크 크기 제한(예: 1GB max), 구형 거리(LOD) 기반 자동 정리, Three.js BufferGeometry 재사용 패턴 적용. 필요시 WebWorker 오프로드. |
| 발표 중 수동 조작 모드에서 드론이 이탈 | 낮음 | 리허설에서 "복귀 트윈" 타이밍을 충분히 연습. 최악의 경우 수동 조작 없이 프리셋 경로만 재생하는 폴백 모드 준비. |
| 심사위원이 "이게 정말 working demo인가" 의심 | 높음 | §7에서 "실제 vs 연출" 명시. 복원은 실촬영 데이터, 경로 지정은 실제 UI 상호작용, 탐지는 향후 개발 로드맵이라 명확히 설명. 데모 중 실시간으로 경로 변경 → 드론이 즉시 반응하는 라이브 시연으로 신뢰도 확보. |
| 좌표계 confusion (GPS vs ENU vs 씬 스페이스) | 중간 | 화면에 "좌표계 범례"(예: "GPS 37.123°N, 126.456°E = ENU (10m, 50m, 2m)")를 표시. README.md 및 코드 주석에서 변환 경로를 명확히. |

---

## 11. 최종 평가로의 자연스러운 확장

이 중간 평가 프로토타입은 **최종 파이프라인의 완성도 높은 뼈대**를 미리 구축하는 작업이다. 최종 평가에서는 최소한의 변경만으로 실제 인프라에 연결된다:

### 11.1 관제탑 확장

| 현재 (중간 평가) | 최종 평가 |
|---|---|
| 로컬 드론 시뮬레이션(경로 재생) | **실제 드론 텔레메트리** (RTK/IMU 포즈 수신) |
| 수동 경로 지정 UI | **드론 fleet 관리 + 우선순위 스케줄링** |
| 방향키 수동 조종(오버라이드) | **실제 드론 제어 명령 송신** |
| 로우파이 포인트클라우드 렌더 | **같은 코드, 더 큰 규모 + 성능 최적화** |

### 11.2 현황판 확장

| 현재 (중간 평가) | 최종 평가 |
|---|---|
| Mock server (로컬 데이터) | **실제 Core HPC 백엔드** (`serverSource.connect(koren_url)`) |
| 오프라인 스플랫 청크 시뮬 | **KOREN을 통한 실시간 스플랫 스트림** (COLMAP→gsplat GPU 재구성) |
| 수동 지정 탐지 마커 | **실제 UNet 추론 결과** (RGB+열화상 4채널, Depth Map 레이캐스팅) |
| 개별 mock 탐지 | **Edge VM 캐시 + 위험구역 세그멘테이션** |

### 11.3 프로토콜 & 인터페이스 재사용성

- **serverSource.ts 인터페이스**: 이미 추상화되어 있어, mock provider에서 실 백엔드(`KOREN+Core HPC`)로 전환 시 `connect(url)` 메서드만 구현하면 됨. 나머지 코드는 변경 불필요.
- **WebRTC 채널**: 중간 평가는 LAN, 최종 평가는 공개 인터넷용 TURN 서버 추가만 필요.
- **상태 공유 프로토콜** (§8.3): 이미 JSON 스키마로 정의되어 있어, 실제 서버/클라이언트 개발 시 명확한 계약 역할.
- **좌표계** (GPS↔ENU): geo.ts/geo.py 미러로 이미 검증되어 있어, 실제 RTK 포즈 입력 시에도 동일 코드 적용 가능.

### 11.4 발표 전략

**심사위원에게 강조할 점**:
- "이 프로토타입은 완성도 높은 프론트엔드 레이어입니다. 중간 평가에서 관제탑·현황판 UX와 3D 복원 파이프라인의 가능성을 증명했으며, 최종 평가에서는 이 뼈대 위에 실제 드론·KOREN·Core HPC를 연결하기만 하면 됩니다."
- "서버 인터페이스, 상태 공유 프로토콜, 좌표계 변환 등 시스템 계약이 이미 정의되어 있어, 실제 파이프라인 연결은 최소한의 노력으로 가능합니다."
- "최종 평가에서는 다음이 추가됩니다: (1) 실제 멀티드론 텔레메트리, (2) Core HPC의 실시간 3D 복원 및 UNet 탐지, (3) KOREN 고속망의 성능 검증, (4) Edge VM 저지연 배포."
